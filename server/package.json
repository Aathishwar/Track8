{
  "name": "track8-push-server",
  "version": "1.0.0",
  "private": true,
  "description": "Serves the Track8 app and sends its break reminders as Web Push, so a reminder no longer depends on the phone keeping the page alive.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "keys": "node generate-vapid.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "web-push": "^3.6.7"
  }
}
